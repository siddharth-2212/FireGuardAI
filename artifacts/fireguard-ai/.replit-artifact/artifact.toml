kind = "web"
previewPath = "/"
title = "FireGuard AI"
version = "1.0.0"
id = "artifacts/fireguard-ai"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18650

[services.development]
run = "pnpm --filter @workspace/fireguard-ai run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/fireguard-ai", "run", "build" ]
publicDir = "artifacts/fireguard-ai/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18650"
BASE_PATH = "/"
